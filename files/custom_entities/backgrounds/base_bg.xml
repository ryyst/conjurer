<Entity>
  <!--
    It's tempting to set the offset so the entity cursor would match better,
    but these are kept at 0 explicitly for the bg spawning to always feel
    precise and natural.
  -->
  <SpriteComponent
    offset_x="0"
    offset_y="0"
  ></SpriteComponent>
</Entity>
